'use client';

// Unified guard — re-exports role-guard.tsx (the canonical implementation).
// All imports to RoleGuard continue to work. Both allowedRoles and minRole props supported.
export { default } from './role-guard';
